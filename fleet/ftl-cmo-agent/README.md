# FTL CMO agent — senior-dev / CI scaffold

Copy `.github/` and `scripts/` to repo root when adopting the fleet pack.

Set `REPO_AGENT_ID=cmo` in CI and add `ftl.agentId` to `package.json`.

See `../../docs/architect/claude-senior-dev-loop.md`.
