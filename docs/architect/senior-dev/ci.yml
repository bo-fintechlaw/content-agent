name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: ftl-content-agent

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ftl-content-agent/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Stage vendor pin manifest
        run: |
          mkdir -p vendor/ftl-agent-core
          cp ../docs/architect/rollout/ftl-agent-core.package.json vendor/ftl-agent-core/package.json

      - name: Verify agent-core pins (0.2.2)
        run: node scripts/verify-agent-core-pins.mjs
